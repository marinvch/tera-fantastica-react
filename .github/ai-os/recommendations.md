# AI OS Recommendations — tera-fantastica-react

> Auto-generated by AI OS based on detected stack. Refreshed on every `--refresh-existing` run.

## Lifecycle Orchestration (Define → Plan → Build → Verify → Review → Ship)

AI OS embeds slash-command prompts for each lifecycle phase. Use them in order:

| Phase | Prompt | Purpose |
| ----- | ------ | ------- |
| Define | `/define` | Structure feature intent, scope, constraints, and success criteria |
| Plan | `/plan` | Break the feature into ordered, dependency-aware implementation tasks |
| Build | `/build` | Execute one task at a time with minimal, convention-compliant changes |
| Verify | `/verify` | Check implementation against success criteria and run tests |
| Review | `/review` | Severity-tagged code review (Critical → Required → Optional → FYI) |
| Ship | `/ship` | Pre-ship checklist — tests, secrets scan, changelog, version bump |

> These prompts are available as VS Code Copilot slash commands via `.github/copilot/prompts.json`.

## Recommended VS Code Extensions

- [`dsznajder.es7-react-js-snippets`](https://marketplace.visualstudio.com/items?itemName=dsznajder.es7-react-js-snippets) — for `react`
- [`burkeholland.simple-react-snippets`](https://marketplace.visualstudio.com/items?itemName=burkeholland.simple-react-snippets) — for `react`
- [`ms-vscode.vscode-typescript-next`](https://marketplace.visualstudio.com/items?itemName=ms-vscode.vscode-typescript-next) — for `TypeScript`

**Install all at once:**
```bash
code --install-extension dsznajder.es7-react-js-snippets
code --install-extension burkeholland.simple-react-snippets
code --install-extension ms-vscode.vscode-typescript-next
```

## Agent Skills to Install

- **react** — for `react`
- **vercel-react-best-practices** — for `react`
- **context7** — for `react`

**Install via skills CLI** (source-based form `<source>@<skill>`):
```bash
npx -y skills add <source>@react -g -a github-copilot
npx -y skills add vercel-labs/agent-skills@vercel-react-best-practices -g -a github-copilot
npx -y skills add intellectronica/agent-skills@context7 -g -a github-copilot
```

> ⚠️  Skills without a known source (`react`): find the GitHub repo hosting the skill and replace `<source>` before running.

## Universal Skills (Optional)

> These skills are useful for any project and are not specific to the detected stack.

- **find-skills** — general purpose

**Install via skills CLI** (source-based form `<source>@<skill>`):
```bash
npx -y skills add vercel-labs/skills@find-skills -g -a github-copilot
```

---
*Generated at 2026-05-10T05:13:35.049Z by AI OS*