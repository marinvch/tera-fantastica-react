---
applyTo: "src/pages/**, src/components/**"
---
# Frontend Rules — tera-fantastica-react
- Use React (Vite) conventions for all UI components
- Prefer shared components in the detected components directory over new one-offs
- All component props must be typed (no `any`)
- Component files: PascalCase (e.g. `MyButton.tsx`)